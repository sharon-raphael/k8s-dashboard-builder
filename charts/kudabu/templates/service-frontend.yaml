apiVersion: v1
kind: Service
metadata:
  name: {{ include "kudabu.fullname" . }}-frontend
  labels:
    {{- include "kudabu.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.frontendPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kudabu.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
